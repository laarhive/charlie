{
  op: "play.start",
  params: {
    speed: 1,

    routingByStreamKey: {
      "*::presenceRaw:*::presence": "device"
    },

    rewriteTs: true,

    isolation: {
      blockDevices: true,
      unblockOnStop: true,
      owner: "playback",
      reason: "presence-replay"
    }
  }
}
