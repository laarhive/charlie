

* mode-based selective activation (rpi4/win11)
* virtual buttons that run through the real button pipeline
* publishAs aliasing working exactly as intended
* CLI support (virt press <id> [ms]) with completion and help
